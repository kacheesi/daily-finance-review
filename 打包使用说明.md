# 📦 每日金融复盘系统 — 打包分发说明

本包用于在**其他电脑**上运行每日金融复盘。提供两种使用方式，任选其一。

---

## 方式一：免安装 exe 版（推荐，适合小白）

### 文件
```
每日复盘.exe        ← 主程序（双击运行）
```

### 使用步骤
1. 把 `每日复盘.exe` 放到任意文件夹（比如 D:\每日复盘\）
2. **双击 `每日复盘.exe`**
3. 黑色窗口自动运行（采集 → 分析 → 生成报告，约 1-2 分钟）
4. 完成后自动打开浏览器显示当日报告
5. 报告文件保存在 exe 同目录的 `data\reports\daily_report.html`

### 注意事项
- **需要联网**（数据来自东方财富/腾讯/新浪/CoinGecko）
- Windows 10/11 可直接运行，无需安装 Python
- 若提示"Windows 已保护你的电脑"，点「更多信息」→「仍要运行」（自己打包的程序，放心）
- 首次运行生成 data 文件夹，属正常现象

---

## 方式二：源码包（适合有 Python 基础或要二次开发）

### 文件
```
每日复盘_源码包.zip
```

### 使用步骤（Windows）
1. 解压到任意目录（如 `C:\每日复盘`）
2. 安装 Python 3.12：https://www.python.org/downloads/（安装时勾选 "Add Python to PATH"）
3. 打开命令行（Win+R → 输入 cmd → 回车）：
   ```cmd
   cd /d C:\每日复盘
   pip install -r requirements.txt
   ```
4. 运行：
   ```cmd
   python run_daily.py
   ```
5. 报告生成在 `data\reports\daily_report.html`

### 常用参数
```cmd
python run_daily.py --force            :: 强制重新采集
python run_daily.py --date 2026-08-19  :: 指定日期
```

### 修改自选股
编辑 `config\watchlist.json`（格式：`{"watchlist":[{"code":"512170","name":"医疗ETF"}]}`）

---

## 常见问题

| 问题 | 解决 |
|---|---|
| 运行失败/报错 | 多为网络问题，重试一次；检查是否能访问东方财富等网站 |
| 报告部分数据缺失（—） | 数据源临时不可用，自动降级，次日一般恢复 |
| exe 被杀毒软件拦截 | 添加信任（程序为本地生成，无任何联网外行为） |
| 想要每天自动运行 | 参照项目内《使用说明.md》配置 Windows 任务计划（每日 17:05） |

---

*打包时间：2026-08-19 · 系统：Windows 10/11 · Python 3.12*
