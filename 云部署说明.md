# ☁️ 云端部署说明 — GitHub Actions + Pages（免费）

把每日复盘系统搬到 **GitHub 云端**：每天 17:00 自动运行（不需要开电脑）、生成报告并发布到网页，**手机/平板/电脑任何浏览器都能看**。全程免费。

## 效果

```
每天 17:00（北京时间）
   ↓ GitHub Actions（免费云端服务器）
自动采集 → 分析 → 生成报告
   ↓ 自动发布
https://<你的GitHub用户名>.github.io/<仓库名>/   ← 手机浏览器打开即看
```

## 部署步骤（只需做一次，约 10 分钟）

### 第 1 步：创建 GitHub 仓库

1. 打开 https://github.com/new
2. Repository name 填：`daily-finance-review`（或其他名字）
3. 选 **Public**（免费无限流量；隐私选 Private 也免费，每月有运行额度）
4. 不要勾选任何初始化选项（README/.gitignore 都不要），点 Create

### 第 2 步：推送代码（推荐网页上传，不需要安装任何东西）

**网页上传（重要：拖「文件夹里面的内容」，不要拖文件夹本身）**

1. 浏览器登录 github.com，进入你的仓库页面（`https://github.com/你的用户名/daily-finance-review`）
2. 点 **Add file → Upload files**
3. 打开本地文件夹 `C:\Users\Administrator\WorkBuddy`，双击进入 `每日复盘` 文件夹，**全选里面的内容**（`config`、`scripts`、`src` 三个文件夹 + `run_daily.py`、`requirements.txt`、`README.md` 等，`data` 文件夹不要选），**拖进上传区域**
   - 提示：如果看不到 `.github` 隐藏文件夹，先做：资源管理器 → 顶部"查看" → 勾选 **"隐藏的项目"**，再全选
4. 点绿色按钮 **Commit changes**
5. **再新建一个自动运行配置文件**：点 **Add file → Create new file**，文件名填：
   ```
   .github/workflows/daily_report.yml
   ```
   然后把下面这段内容**全部复制粘贴**进去（见本文件末尾"workflow 文件内容"），点 **Commit new file**

> ⚠️ 切记：上传时是选中文件夹**里面的文件**，不是把文件夹本身拖进去。
> 如果拖了文件夹本身，所有文件会变成在 `每日复盘/` 子目录里，自动运行会失效（需按文末"常见问题：文件被套在子文件夹里"修复）。

**备用方式：双击一键推送**（需本机装好 git 并完成过 GitHub 登录）——见 `一键推送.bat`。

### 第 3 步：开启 GitHub Pages

1. 打开：`https://github.com/<用户名>/daily-finance-review/settings/pages`
2. **Build and deployment → Source** 选择 **GitHub Actions**（不要选 Deploy from a branch）

### 第 4 步：手动触发第一次运行

1. 打开：`https://github.com/<用户名>/daily-finance-review/actions`
2. 左侧点「每日金融复盘」→ 右侧 **Run workflow** 按钮 → 运行
3. 等 2-4 分钟（首次需装依赖），Actions 变绿即成功

### 第 5 步：查看报告

访问：**https://<用户名>.github.io/daily-finance-review/**

- 以后每天 17:00 自动更新（周末自动跳过，保留上次报告）
- 手机收藏这个网址，每天收盘后直接看
- 历史报告在 `/reports/2026-08-18.html` 这样的路径

## 云端与本地差异说明

| 项目 | 云端（GitHub Actions） | 本地 |
|---|---|---|
| 运行时间 | 每天 17:00 自动 | 每天 17:00（需开机） |
| AI 分析 | 规则化总结（无 LLM） | 规则化 / WorkBuddy 自动化含 AI |
| 数据源 | 腾讯/新浪优先（海外可达性高），东财兜底 | 东财优先（含主力资金/A50），最全 |
| 主力资金/A50 | 可能降级缺失（东财不可达时） | 完整 |
| 自选股管理 | 改 `config/watchlist.json` 后推送到仓库 | 网页 8787 / 本地 |

> 说明：GitHub 服务器在海外，东财部分接口可能不通，系统会自动降级到腾讯/新浪并保留全部核心功能（指数/自选股/期货/币/评分/评级）。若发现云报告某些字段缺失，属正常降级，不影响使用。

## 修改自选股池（云端）

1. 本地编辑 `config/watchlist.json`（或本地管理页修改）
2. 推送：
```bash
git add config/watchlist.json && git commit -m "update watchlist" && git push
```
3. 云端 Actions 页手动 Run 一次（或等第二天自动）

## 常见问题

- **Actions 报错**：点开失败任务看日志；多数是网络/依赖问题，重跑一次即可
- **页面打不开**：确认第 3 步 Source 选的是 GitHub Actions，且 Actions 已成功
- **手机访问慢**：GitHub Pages 国内访问一般可用，偶尔慢可刷新；如需更稳可后续加 Cloudflare 加速（免费）
- **想用自己域名**：Settings → Pages → Custom domain 绑定即可（需域名）
- **Actions 页面没有 Run workflow 按钮**：说明 `.github/workflows/daily_report.yml` 没在仓库根目录正确位置（可能上传时把文件夹整个拖进去了）。修复见下方"文件被套在子文件夹里"
- **文件被套在子文件夹里**（根目录只有一个"每日复盘"文件夹）：GitHub 网页无法一键移动文件夹，最省事是**删库重建**：
  1. 仓库 Settings → 拉到最底部 Danger Zone → Delete this repository → 输入仓库名确认删除
  2. 重新到 https://github.com/new 创建同名仓库（Public）
  3. 按"第 2 步"重新上传（这次选文件夹**里面的内容**）+ 新建 workflow 文件

## workflow 文件内容

> ⚠️ **不要从下面代码块复制**（会带进 ``` 符号导致报错）。
> 请打开项目文件夹里的 **`workflow内容.txt`**，全选复制里面全部内容后粘贴。

在仓库里新建文件 `.github/workflows/daily_report.yml` 时，粘贴上述 txt 的内容即可。

（下方代码块仅供参考，不可直接复制）

```yaml
name: 每日金融复盘
on:
  schedule:
    - cron: "0 9 * * *"
  workflow_dispatch:
permissions:
  contents: write
  pages: write
  id-token: write
concurrency:
  group: "pages"
  cancel-in-progress: false
jobs:
  build-report:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: 安装依赖
        run: pip install -r requirements.txt
      - name: 下载图表库
        run: python scripts/seed_vendor.py
      - name: 运行每日复盘
        env:
          DAILY_REVIEW_CLOUD: "1"
          TZ: "Asia/Shanghai"
        run: python run_daily.py --force
      - name: 准备发布目录
        run: |
          mkdir -p _site/reports _site/data/vendor
          cp data/reports/daily_report.html _site/index.html
          cp data/reports/*.html _site/reports/ 2>/dev/null || true
          cp data/vendor/echarts.min.js _site/data/vendor/
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: _site
      - name: 部署到 GitHub Pages
        uses: actions/deploy-pages@v4
```
